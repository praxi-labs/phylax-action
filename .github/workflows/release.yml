name: Release

on:
  push:
    tags: ['v*']

permissions: {}

jobs:
  release:
    name: Release
    uses: praxi-labs/ci/.github/workflows/action-release.yml@v1
    with:
      sibling-package: phylax-sdk-js
    permissions:
      contents: write
